{
  "permissions": {
    "allow": [
      "Bash(git *)",
      "Skill(update-config)",
      "Skill(update-config:*)"
    ]
  },
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/efrain/Documents/efrain-fm && git status --porcelain | grep -q . && git add -A && MSG=$(node scripts/commit-msg.js) && git commit -m \"$MSG\" && git push origin main 2>/dev/null || true",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
